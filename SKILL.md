# Agent-Overseer (AI 包工头)

## 0. 简介 (这是干嘛的？)
Agent-Overseer (AI 包工头) 是专门为管理和监控后台长时间运行的 AI 编程助手（如 Codex CLI 等）而设计的原生 OpenClaw Skill。

当你在终端里运行 AI 编程助手执行大任务时，没人盯着它可能会陷入死循环、内存泄漏或者卡死。这个工具就是你的“监工”：
- **后台托管**：把 AI 丢进后台 tmux 运行并死死盯着它。
- **自动防呆**：遇到死循环、报错卡死或进程崩溃时，自动发现并重启它。
- **智能汇报**：平时不打扰你，只有在完成任务（里程碑）或彻底搞不定（严重阻塞）时，才会推送到飞书（或当前聊天平台）向你告警。
- **远程干预**：你可以直接通过聊天窗口发指令，打断正在发呆的 AI，并告诉它下一步该怎么做。

## 1. 核心特性
1. **OS级资源接管**：通过 `ulimit -v 2097152` 限制最大可用内存 (2GB)，配合后台 `tmux` 会话运行。
2. **状态与语义监控**：利用底层文件系统 (`find` 命令 + `mtime`) 追踪文件的实际修改进度。如果发现 AI 连续发呆（文件长期未发生变动），自动触发“鬼打墙”告警。
3. **原生通知与自动目标抓取**：深度结合 OpenClaw 生态！无需用户手动配置复杂的网络密钥，只要在聊天窗口触发（通过向进程注入 `OPENCLAW_CURRENT_TARGET` 环境变量），**包工头会自动抓取当前对话的用户 ID**，当后台发生异常或每 10 分钟发送简报时，能自动推送到原聊天窗口！

## 2. 严格红线 (防呆)
- **四不交**：绝对禁止提交敏感凭证、依赖库/产物、运行日志/测试数据、系统与IDE文件。
