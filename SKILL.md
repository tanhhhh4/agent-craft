# Agent-Overseer (AI 包工头)

## 0. 简介 (这是干嘛的？)
Agent-Overseer (AI 包工头) 是专门为管理和监控后台长时间运行的 AI 编程助手（如 Codex CLI 等）而设计的原生 OpenClaw Skill。

当你在终端里运行 AI 编程助手执行大任务时，没人盯着它可能会陷入死循环、内存泄漏或者卡死。这个工具就是你的“监工”：
- **后台托管**：把 AI 丢进后台 tmux 运行并死死盯着它。
- **自动防呆**：遇到死循环、报错卡死或进程崩溃时，自动发现并重启它。
- **智能汇报**：平时不打扰你，只有在完成任务（里程碑）或彻底搞不定（严重阻塞）时，才会推送到飞书向你告警。
- **远程干预**：你可以直接通过聊天窗口发指令，打断正在发呆的 AI，并告诉它下一步该怎么做。

## 1. 设计理念与核心架构 (v2.0)
这是一个基于 OpenClaw 原生通道的轻量级 AI 编程助手监控 Skill。

### 核心机制
1. **OS级资源接管**：放弃臃肿的 Docker，启动时通过 `ulimit -v 2097152` 限制最大可用内存 (2GB)，配合后台 `tmux` 会话运行。
2. **状态与语义监控**：通过 OpenClaw `process` 轮询，定期检查 `tmux capture-pane` 的最新输出日志，判断是否出现“鬼打墙（死循环/幻觉）”。
3. **事件驱动通知**：仅在“关键里程碑”和“严重阻碍（重试超时、进程被杀）”时向飞书推送告警。
4. **原生聊天指令注入**：直接在飞书发送聊天指令打断 AI 发呆，通过 `tmux send-keys` 注入新指令 (C-c -> send-keys -> C-m)。

## 2. 严格红线 (防呆)
- **四不交**：绝对禁止提交敏感凭证、依赖库/产物、运行日志/测试数据、系统与IDE文件。
