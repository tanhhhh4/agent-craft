请重构 `skills/agent-overseer/index.js` 的监控逻辑，解决以下架构缺陷：

1. 核心违规：使用正则与文本截取监控
代码中 `startPolling` 方法使用了 `tmux capture-pane -pt ${taskName} -S -100`。这违背了 2.0 架构中“坚决不用字符串匹配”的红线。大模型的终端输出不可控，截取日志匹配极易造成误判和崩溃。

2. 整改要求：引入文件系统（FS）级语义监控
- 移除 `capture-pane` 的相关逻辑。
- 将监控逻辑改为轮询读取工作区中**最新被修改的文件（mtime）**（例如使用 `find` 命令并避开 `node_modules` 和 `.git`）。
- 维护一个计数器，如果连续 3 次轮询（约1分半钟）发现 AI 都在死磕同一个文件且未有新进展，才判定为死锁/鬼打墙并触发 `triggerAlert`。

3. 其他部分（`tmux send-keys` 干预机制、`ulimit` 资源限制等）保持不变，不要暴露 HTTP 接口。